#include <stdio.h>
Int main() |
Int numeros [10];
ant c, mator = -9999;
for(с = 0; с < 10; c++) ( printf("Digite o Número da Posição %d: ". C);
scant ("%a", &numeros [c]);
1f (numeros[c] > maior) (
maior = numeros[c]:
else(
print (Mador Valor: Nd", maior);
rotura 9;
}
